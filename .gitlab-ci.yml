image: alpine:latest

pages:
  script:
    - mkdir -p .public
    - rsync -av --exclude=".git" --exclude=".gitlab-ci.yml" ./ .public/
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main
  cache:
    paths:
      - public
